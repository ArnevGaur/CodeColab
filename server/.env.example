# Server Configuration
PORT=5005
JWT_SECRET=your_jwt_secret_here

# Database Configuration
# Provide a real MongoDB URI (e.g., from MongoDB Atlas) for persistent storage.
# If left empty, the project will use mongodb-memory-server (transient).
MONGODB_URI=
